<% content_for :title, t('.title') %>

<%= render layout: "layouts/custom_layout" do %>
  <%= render 'form', tier: @tier, tier_categories: @tier_categories, tier_ranks: @tier_ranks, items: @items %>
<% end %>
